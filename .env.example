# Aerovia requires no runtime environment variables, accounts, API keys or secrets.
# This file is copied to the ignored .env by local setup for an explicit configuration contract.
# Ports, input paths, compute device and seeds are CLI options; see docs/guides/guides.md.
# Never place a secret in VITE_* variables: frontend build variables become public JavaScript.
