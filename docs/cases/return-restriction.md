# Return-shaft maintenance

ID `return-restriction`; 45 nodes, 57 airways. The three-level reference network has `return-shaft-0` resistance multiplied by eight, from 0.015 to 0.12 Pa·s²/m⁶. Workings and targets are unchanged.

Compare how a common upper return restriction affects all three horizons. Save a baseline and test bounded speed adjustment, then inspect fan demand and all target deficits. Because the restriction is shared, changing a single downstream working may have limited influence on the bottleneck. Complete closure can be attempted interactively; the validator rejects any closure configuration that isolates an internal node from every pressure boundary.

The change is an equivalent maintenance restriction. It is not a construction schedule or certified temporary ventilation plan.
