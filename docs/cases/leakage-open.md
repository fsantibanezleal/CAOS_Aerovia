# Open leakage paths

ID `leakage-open`; 45 nodes, 60 airways. The three-level base network gains one direct intake-to-return crosscut at each horizon. Their resistances are 0.09, 0.11 and 0.13 Pa·s²/m⁶, with no working targets on the bypass itself.

Select each `leakage-*` airway and compare its flow with the nearby production circuits. The visual highlights a physically meaningful shortcut: the main fan's volume can increase while the productive target distribution worsens. Save this baseline, raise leakage resistance, and inspect all production shortfalls and electrical demand. The paired sealed case applies the same three changes reproducibly.

These bypasses are authored equivalent airway resistances. They do not model a measured door leakage curve or transient opening schedule.
